{
  "name": "rork-downloader-server",
  "version": "1.0.0",
  "description": "Local server to receive project JSON and create folder structure",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
